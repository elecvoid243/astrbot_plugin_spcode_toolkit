# Task 6 Report — file-discard-hunk route registration

Generated by: discard_hunk_impl_6  
Timestamp: 2026-07-06 22:46 CST

## Status

Completed route/test implementation and committed. Targeted e2e suite and ruff pass. Full regression was executed and has one pre-existing/unrelated failure outside Task 6 scope.

## Commit

- SHA: `efb3320df7fdc0f131ac62b3535759fd76b9a491`
- Message: `feat(webapi): register /spcode/file-discard-hunk route (v2.16.0, 20th endpoint)`

## Files changed

- `tools/webapi/__init__.py`
  - Added `file_discard_hunk` module import.
  - Added `/spcode/file-discard-hunk` POST route after `/spcode/file-restore`.
  - Added `handle_post_file_discard_hunk` to `HANDLERS`.
  - Added `file_discard_hunk` to `__all__`.
  - Updated route-count docstrings from 19 to 20.
- `tests/test_webapi_end_to_end.py`
  - Renamed route table test to `test_routes_table_has_twenty_endpoints`.
  - Added `/spcode/file-discard-hunk` to expected route set.
  - Updated method sanity count to 9 GET + 11 POST.
  - Updated registration call-count tests to 20.
  - Added `test_handlers_dict_has_discard_hunk_entry`.
  - Added `test_wrap_injects_body_for_post`.

## Verification

Commands were run with the AstrBot dev environment on PATH (`D:\anaconda3\envs\astrbot;D:\anaconda3\envs\astrbot\Scripts`) so `astrbot.api.web` resolves correctly.

1. `pytest tests/test_webapi_end_to_end.py -v`
   - Result: PASS — `40 passed, 2 warnings in 1.66s`.
2. `pytest tests/ -v`
   - Result: FAIL — `1 failed, 1052 passed, 5 skipped, 44 warnings in 290.68s`.
   - Failure: `tests/test_todo_guidance_inject.py::test_inject_when_todo_create_enabled` expects `"Do NOT batch"` in `TODO_GUIDANCE`; current `tools/_guidance_text.py` comment mentions it but the guidance string does not include it. This is unrelated to Task 6 route registration.
3. `ruff check .`
   - Result: PASS — `All checks passed!`

## Notes

- A first plain shell run of `pytest tests/test_webapi_end_to_end.py -v` used base Anaconda `pytest` and failed during import because that interpreter resolves an older installed AstrBot package without `astrbot.api.web`. Re-running under the AstrBot dev environment passed.
- Full regression log saved locally at `.superpowers/sdd/pytest-full.log`.
